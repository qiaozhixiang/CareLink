# CareLink 项目简介

## 一、项目概述

CareLink 是一套面向老年人及其家属的智慧关怀服务系统。项目围绕居家养老与家庭照护场景，将日程安排、健康记录、日常提醒、紧急求助、家庭协作和远程关怀整合到同一套移动应用与服务端系统中。

许多家庭在照护长者时会遇到信息分散、提醒不及时、异地难以了解近况、突发情况响应慢等问题。CareLink 通过长者端和家属端的双角色设计，将“日常事项有提醒、状态变化可感知、异常事件能响应、家庭成员可协作”串联为闭环：长者使用简洁直观的页面完成打卡和求助，家属通过总览、告警、日历和成员管理掌握情况并安排照护。

项目由 Android 客户端、Spring Boot 后端和 MySQL 数据库组成。客户端作为用户日常使用入口，后端负责身份认证、业务规则、数据读写、文件处理、实时通信与版本管理，数据库保存家庭关系、日程、记录、告警等核心业务数据。

## 二、目标用户与角色

### 1. 长者

长者是服务的主要使用对象。客户端为其提供首页、今日打卡、紧急求助、家庭联系、日程和设置等入口，减少复杂操作。长者可查看当天待办、完成服药、饮水、散步、血压、血糖和睡眠等项目的记录；也可在身体不适或发生紧急情况时快速发起求助。

### 2. 家属

家属负责关注、协作和响应。家属端可查看家庭总览、待处理异常、日历日程、关怀备注、轮值安排和家庭成员；可创建或编辑日程、处理告警、管理家庭关系，并获取长者主动共享的位置与健康报告。

### 3. 管理员

后端提供应用版本管理能力，包括 APK 上传、版本信息维护、安装包下载和版本列表查询，支持后续应用发布与强制更新策略。

## 三、核心功能

### 1. 账号与身份体系

- 支持邮箱验证码注册、登录、退出登录和密码重置。
- 支持微信登录接口，并可在未配置微信参数时使用常规账号登录。
- 登录后可选择长者或家属角色，服务端通过 JWT 维护身份认证状态。
- 用户可查询个人资料、更新昵称等资料并上传头像。
- 长者端和家属端分别进入不同的信息架构和页面导航，避免将不必要的操作暴露给长者。

### 2. 家庭建立与成员协作

- 家属可创建家庭、通过邀请码邀请成员加入，并校验邀请码有效性。
- 可查询家庭基本信息、全部成员和家庭中的长者成员。
- 支持成员移除、创建者转让、退出家庭和解散家庭等管理操作。
- Android 端支持家庭邀请码深度链接，可从 `carelink://family` 或预留的 HTTPS 链接进入家庭绑定流程。

### 3. 日程与就诊安排

- 家属可创建、查询、修改和删除日程，并按时间范围检索。
- 支持更新日程状态，适用于复诊、用药、活动、家庭事务等安排的跟踪。
- 客户端提供日历页面和日程编辑页面，后端提供日程列表及时间范围查询接口。
- 系统同时提供农历和月份宜忌查询接口，为日历展示扩展传统日期信息。

### 4. 今日打卡与健康习惯管理

- 后端可查询打卡任务、提交打卡记录并查询当天记录。
- 客户端预设服药、饮水、散步、血压、血糖、睡眠等高频照护事项。
- 本地数据层包含打卡任务与打卡记录实体、DAO 和同步能力，可为弱网或离线使用提供数据承载基础。
- WorkManager 任务调度用于执行提醒、同步和告警检测等后台工作。

### 5. 健康数据与异常告警

- 长者端可上报健康数据，家属端可查询家庭成员的最新健康报告。
- 客户端包含健康分析服务与跌倒检测前台服务，用于支持健康趋势分析与异常识别。
- 可主动触发紧急事件，家属端可查看全部告警、待处理告警并标记处理结果。
- 告警中心与家庭总览把异常信息集中展示，帮助家属优先处理紧急事项。

### 6. 位置共享与地图查看

- 长者可上报当前位置，并开启或关闭位置共享。
- 家属可查询家庭成员最近位置；成员位置也可由家属侧进行更新。
- 客户端提供地图预览、成员位置可视化和位置共享辅助模块。
- 该功能依赖定位权限，实际使用应以长者知情同意为前提，并遵循最小必要原则。

### 7. 关怀记录、陪伴提醒与轮值

- 家属可创建、查看和删除关怀备注，形成持续的照护记录。
- 可发送陪伴提醒、上传提醒图片、查看已发送与未读提醒，并将提醒标记为已读。
- 客户端提供轮值页面、轮值数据模型和本地持久化组件，支持家庭成员分担照护责任。
- 通过备注、提醒和轮值三个维度，减少照护信息只存在于个人聊天记录中的问题。

### 8. 家庭联系、语音与智能对话

- 长者端可查看家庭联系人，便于在需要时快速联系家人。
- 客户端提供语音播报、方言语音辅助、字体缩放和主题辅助工具，强化使用可达性。
- 工程包含长者端、家属端智能对话页面、聊天消息模型及 AI 数据仓库接口，为情感陪伴和问题咨询提供交互入口。
- AI 对话属于辅助能力，不应用于替代医疗、急救或专业护理判断。

### 9. 远程协助与应用更新

- 长者端和家属端分别提供远程协助发起与查看页面。
- 系统包含屏幕采集前台服务和 WebSocket 管理模块，用于实现远程协助所需的实时连接能力。
- 后端支持 APK 上传、下载、版本查询和版本更新；客户端提供更新管理器和更新弹窗。
- 远程协助、屏幕采集和更新安装均涉及高敏感权限，产品上线前应补充明确授权、操作提示、审计记录和撤销机制。

## 四、信息架构与交互设计

### 1. 双角色导航

长者端采用以任务完成和紧急操作为中心的导航结构：

```text
首页 -> 今日打卡 -> 紧急求助 -> 家属联系 -> 日程 -> 设置
```

家属端采用以了解状态和组织照护为中心的导航结构：

```text
总览 -> 异常中心 -> 日历 -> 备注 -> 轮值 -> 成员 -> 设置
```

同一账号可在完成登录后选择角色。这样的结构避免了长者端出现过多管理功能，也让家属能在总览和异常中心优先看到需要处理的信息。

### 2. 适老化设计原则

- **低认知负担**：长者端围绕“今天要做什么”“有事怎么办”“怎么联系家人”组织功能，缩短完成路径。
- **明确反馈**：打卡、提醒和告警均应呈现清晰的状态和结果，降低不确定感。
- **多通道辅助**：字体缩放、语音播报、方言语音、通知、振动和精确提醒可共同提升可用性。
- **紧急优先**：SOS 与紧急求助应位于容易找到的位置，告警事件在家属端集中处理。
- **家庭协作**：家庭邀请码、成员管理、轮值和备注使多位家属能够共享照护信息。

### 3. 关键业务闭环

**日常照护闭环**：家属安排日程或提醒 -> 长者查看任务并完成打卡 -> 客户端同步记录 -> 家属在总览、日历或备注中了解完成情况。

**异常响应闭环**：客户端检测到异常或长者主动求助 -> 后端创建告警事件 -> 家属端出现待处理告警 -> 家属联系、处理并确认事件状态。

**家庭绑定闭环**：家属创建家庭 -> 生成或分享邀请码 -> 长者或其他家属加入 -> 成员关系建立 -> 在授权范围内共享日程、提醒、健康信息和位置。

## 五、技术架构与工程设计

### 1. Android 客户端

客户端采用 Android 原生应用结构，源码位于 `android/`。工程按职责拆分为以下层次：

- `ui/`：登录、角色选择、长者端、家属端、日历、告警、备注、设置、地图等页面与 ViewModel。
- `data/remote/`：接口定义、认证拦截器、请求与响应 DTO。
- `data/local/`：Room 数据库、实体、DAO 和偏好设置，用于本地数据保存。
- `data/repository/`：认证、日程、告警、家庭、健康、位置、提醒、备注等业务数据仓库。
- `domain/`：告警引擎、今日任务等核心模型与用例。
- `worker/`：提醒、同步和告警检测等后台任务调度。
- `remote/`：WebSocket、屏幕采集和远程协助相关模块。
- `utils/`：网络错误处理、定位共享、地图成员可视化、字体、主题、语音与应用更新等通用能力。

应用申请网络、通知、定位、活动识别、传感器、电话、短信、精确闹钟、前台服务和媒体投影等权限。这些权限分别为连接服务、提醒、位置共享、跌倒检测、紧急联系与远程协助提供基础；上线产品应按功能按需申请，并在使用前完成清晰告知。

### 2. Spring Boot 后端

后端源码位于 `backend/`，使用 Java 17 与 Spring Boot 3.2。主要职责包括：

- 使用 Spring Web 提供 REST API。
- 使用 Spring Data JPA 访问 MySQL，实体与仓库覆盖用户、家庭、日程、打卡、提醒、备注、位置、告警与应用配置等领域数据。
- 使用 Spring Security 和 JWT 完成身份认证与接口访问控制。
- 使用 Spring WebSocket 提供实时通信基础能力。
- 使用 Spring Mail 发送邮箱验证码和找回密码邮件。
- 使用文件存储配置处理头像、提醒图片和 APK 上传。
- 通过统一异常处理、跨域配置、WebSocket 缓冲配置和应用健康检查提高服务可维护性。

服务默认运行在 `8080` 端口，接口按业务划分为 `/api/auth`、`/api/family`、`/api/appointments`、`/api/checkin`、`/api/health`、`/api/location`、`/api/alert`、`/api/notes`、`/api/family/reminders`、`/api/calendar`、`/api/app` 与 `/api/admin` 等路径。

### 3. 数据与部署边界

MySQL 初始化脚本位于 `database/carelink_db.sql`，数据库包含用户、家庭、日程、打卡、关怀记录、陪伴提醒、位置共享、告警与应用配置等业务表。应用配置中预留了短信、邮件、推送、对象存储、地图、Redis 和微信登录等第三方服务参数。

已构建的 APK 与 JAR 分类存放在 `artifacts/`。该目录用于交付，不参与源码构建；根目录 `.gitignore` 会忽略构建产物、缓存、日志和本地配置，避免将环境数据与凭据提交到版本库。

## 六、安全与隐私设计要点

CareLink 涉及位置、健康、联系人、远程协助等个人敏感信息。项目设计和后续上线应持续遵循以下原则：

- 通过登录认证、JWT 和角色区分控制接口访问范围。
- 位置共享应由长者主动开启，并可随时关闭；家属仅查看已授权成员。
- 远程协助和屏幕采集须经用户实时、明确授权，且应提供显著的进行中提示。
- 健康分析和智能对话仅用于辅助提醒，不替代医疗建议与紧急救援流程。
- 生产环境不得使用仓库内的本地默认凭据，应使用环境变量或受保护的生产配置管理密钥。
- 应记录告警处理、远程协助和敏感数据访问等重要操作，以便审计与问题追踪。

## 七、当前工程说明

项目已具备 Android 客户端源码、Spring Boot 后端源码、数据库脚本以及已构建的 APK/JAR。当前 Android 目录包含顶层 Gradle 配置和 `app/src` 源码，但缺少 `android/app/build.gradle` 模块构建脚本，因此需要补回该文件后才能在 Android Studio 或命令行中完整构建客户端。后端可基于 JDK 17 和 MySQL 8.0+ 进行本地部署与调试。

CareLink 后续可在完善权限授权、隐私策略、告警通知链路、自动化测试、监控告警与生产化配置后，作为面向家庭照护场景的移动关怀平台持续迭代。
